name: Get Paddle Chinese Api

on: push

jobs:
  Paddle-Chinese-Api:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository code
        uses: actions/checkout@v4
      - name: Run script
        run: |
          chmod +x gen_cn_api.sh
          ./gen_cn_api.sh
